# macOS
.DS_Store
.AppleDouble
.LSOverride

# Metrics and temporary files
.claude-flow/metrics/
.swarm/memory.db

# Backups
*.backup

# SSH credentials (should be in ~/ outside repo, but block just in case)
cfkssh.txt
*ssh*.txt
ssh_password*
