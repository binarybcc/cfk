<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ pageTitle|default('Admin') }}{% endblock %} - CFK Admin</title>

    <link rel="stylesheet" href="{{ baseUrl('assets/css/styles.css') }}">
    <link rel="icon" type="image/x-icon" href="{{ baseUrl('assets/images/favicon.ico') }}">

    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    {% block head_extra %}{% endblock %}
</head>
<body class="admin-body">
    <header class="admin-header">
        <div class="admin-header-content">
            <div class="admin-logo">
                <h1><a href="{{ baseUrl('/admin/dashboard') }}">CFK Admin</a></h1>
                <p class="admin-tagline">Christmas for Kids Administration</p>
            </div>

            <nav class="admin-nav">
                <ul>
                    <li><a href="{{ baseUrl('/admin/dashboard') }}">Dashboard</a></li>
                    <li><a href="{{ baseUrl('/admin/children') }}">Children</a></li>
                    <li><a href="{{ baseUrl('/admin/sponsorships') }}">Sponsorships</a></li>
                    <li><a href="{{ baseUrl('/admin/import') }}">Import/Export</a></li>
                    <li><a href="{{ baseUrl('/admin/reports') }}">Reports</a></li>
                    <li><a href="{{ baseUrl('admin/year_end_reset.php') }}" style="color: #dc3545;">Year-End Reset</a></li>
                </ul>
            </nav>

            <div class="admin-user">
                <span class="user-greeting">
                    Welcome, Admin
                </span>
                <div class="user-actions">
                    <a href="{{ baseUrl('/') }}" class="btn btn-small btn-secondary" target="_blank">View Site</a>
                    <a href="{{ baseUrl('admin/logout.php') }}" class="btn btn-small btn-danger">Logout</a>
                </div>
            </div>
        </div>
    </header>

    <main class="admin-content">
        {# Flash Messages #}
        {% if flash_message is defined %}
            <div class="alert alert-{{ flash_type|default('info') }}">
                {{ flash_message }}
            </div>
        {% endif %}

        {% block content %}{% endblock %}
    </main>

    <footer class="admin-footer">
        <p>Christmas for Kids Admin Panel - <?php echo date('Y'); ?></p>
    </footer>

    {% block scripts %}{% endblock %}
</body>
</html>
