# ============================================================================
# STAGING ENVIRONMENT CONFIGURATION
# ============================================================================
#
# This file is a TEMPLATE for your staging environment configuration.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to `.env` on your STAGING server (NOT in git!)
# 2. Replace all placeholder values with your actual staging credentials
# 3. Set secure file permissions: chmod 600 .env
# 4. Verify .env is in .gitignore (never commit!)
#
# LOCATION: Place this file in the ROOT of cfk-standalone/ directory
#           Same location as .env.example and .gitignore
#
# ============================================================================

# Environment Type
ENVIRONMENT=staging

# Database Configuration (from Nexcess cPanel > MySQL Databases)
DB_HOST=localhost
DB_NAME=your_staging_db_name
DB_USER=your_staging_db_user
DB_PASSWORD=your_staging_db_password

# SMTP Configuration (from Nexcess > Email Accounts or MailChannels)
SMTP_HOST=relay.mailchannels.net
SMTP_PORT=587
SMTP_USERNAME=your_staging_smtp_user
SMTP_PASSWORD=your_staging_smtp_password
SMTP_ENCRYPTION=tls

# Application Settings
APP_DEBUG=true                              # Enable debug mode for staging
BASE_URL=https://staging.cforkids.org/      # Your actual staging URL

# Admin Email (where staging notifications go)
ADMIN_EMAIL=your-email+staging@example.com  # Use +staging for easy filtering

# ============================================================================
# STAGING-SPECIFIC NOTES
# ============================================================================
#
# 1. Keep APP_DEBUG=true to see detailed errors during testing
# 2. Use a different admin email (e.g., +staging suffix) to separate from prod
# 3. Consider using a subdomain: staging.cforkids.org or cforkids-staging.nexcess.net
# 4. Staging database should be separate from production
# 5. Test all changes on staging before deploying to production
# 6. Staging should mirror production as closely as possible (PHP version, etc.)
#
# ============================================================================
# SECURITY REMINDERS
# ============================================================================
#
# ✓ This .env file must NEVER be committed to git
# ✓ Each server has its own .env file (local, staging, production)
# ✓ Set permissions: chmod 600 .env (owner read/write only)
# ✓ Add .env to .gitignore (already done)
# ✓ Use strong, unique passwords for staging (different from production!)
#
# ============================================================================
