# Production-like Environment Configuration for Docker Testing
# This file simulates production environment in Docker/OrbStack
# Copy from this template and adjust for your needs

# Database Configuration (Docker DB container)
DB_HOST=db
DB_NAME=cfk_sponsorship_dev
DB_USER=cfk_user
DB_PASSWORD=cfk_pass

# SMTP Configuration (use your test SMTP or production credentials)
# NOTE: Test with real SMTP to verify email delivery in Docker
SMTP_USERNAME=
SMTP_PASSWORD=

# Application Settings (Production-like)
APP_DEBUG=false
BASE_URL=http://localhost:8082

# Error Reporting (Production mode)
# These will be overridden by docker/php.ini
PHP_DISPLAY_ERRORS=Off
PHP_LOG_ERRORS=On
